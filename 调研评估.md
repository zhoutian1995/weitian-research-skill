---
title: 多平台资料搜索方案调研评估
date: 2026-09-12
status: current
---

# 多平台资料搜索方案调研评估

## 结论

采用“Ego Lite 国内会话 + last30days 海外聚合 + Windows 5070 视频转写”的组合。首轮默认走快速档，视频字幕和评论作为第二阶段，避免 YouTube 链路拖慢所有任务。

## 选型

| 组件 | 用途 | 采用判断 |
|---|---|---|
| mvanhorn/last30days-skill | Reddit、X、YouTube、网页等海外公开来源聚合 | 采用；高星、高活跃、支持 Codex/Grok |
| Ego Lite | 抖音、B站、小红书的真实登录态浏览 | 采用；已实测抖音搜索 |
| 公众号公开文章 | 行业解释、案例、政策和方法论背景 | 采用；与短视频用户体验分层记录 |
| faster-whisper | 视频/音频转文字 | 采用方向；放到 Windows RTX 5070 执行 |
| 小红书第三方爬虫 Skill | 批量抓取、视觉识别、Cookie/风控处理 | 暂不采用；封号和合规风险高 |

## 实测速度

- Ego Lite 抖音搜索“1688 电商痛点”：约 6 秒，登录后可见真实视频卡片。
- last30days 快速档（Reddit、X、网页）：两次本机实测约 4.6 秒和 79.2 秒；最新一次得到 Reddit 1 个 thread，X 因未配置显式授权为 0，网页为 0。延迟受公开源响应和退避策略影响，不能视为稳定 SLA。
- last30days 平衡档（加入 YouTube）：本机一次实测约 114 秒；YouTube 字幕缺失时仍会消耗页面和评论请求时间。

速度结论：国内路由清单生成约 0.03–0.04 秒，但海外公开聚合可能从几秒波动到约 80 秒；不能把“全平台、视频转写、评论富集”设成默认动作。先用快速档判断是否值得继续，再开启 YouTube/转写。

## 风险边界

- 小红书、抖音、B站只读；不点赞、不评论、不关注、不发布。
- 小红书只保留人工登录的 Ego Lite 会话；不导出 Cookie，不做验证码对抗和高频批量抓取。
- last30days 适配层默认关闭隐式浏览器 Cookie 读取；X 必须由用户显式配置授权或使用 Grok Bot 的连接器。
- 所有运行数据和视频转写结果写外接 SSD KnowledgeBase；SSD 不可用时停止，不回退到内置盘或 NAS。

## 当前拆分

1. `skills/research-router/SKILL.md`：平台和风险路由规则。
2. `平台路由.yaml`：国内/海外/技术场景的机器可读路由。
3. `skills/research-router/scripts/资料搜索.py`：生成任务清单；`--speed fast` 为默认，`--speed balanced` 加入 YouTube。
4. `skills/research-router/vendor/`：上游源码快照；维护源在 `/Users/wille/projects/last30days-skill`。

## 下一步

先用抖音、B站、小红书人工会话完成“1688 电商痛点”首轮样本，再把结果与知识库已有的 7 类痛点聚类合并。若连续多轮确实需要更大规模的小红书数据，再单独评估官方 API 或合规数据服务，不先上第三方高权限爬虫。

## BibiGPT API 评估

BibiGPT适合做“拿到视频链接之后的转写、摘要、章节和问答”，不适合替代抖音、小红书、B站和 X 的发现与检索。官网公开说明支持 B 站、YouTube、抖音、小红书和 X 等链接；其 OpenAPI 提供 URL 总结、字幕提取、异步任务和自定义提示词接口。

API 目前仍标注为 beta，且官方文档说明 API 与官网功能可能不同。小红书和抖音在平台保护触发时可能无法服务端处理，不能把“支持平台”理解成“稳定可批量采集”。API 更适合作为第二阶段视频证据补充。

建议先用免费额度做 10 条混合样本测试，记录成功率、等待时间、字幕完整度、时间戳准确度和每分钟成本；达到稳定阈值后再购买按量额度。API Token 只放环境变量或密钥管理器，不进入仓库。

## video-transcript 项目评估

[Backtthefuture/video-transcript](https://github.com/Backtthefuture/video-transcript) 的定位是本地 FunASR 转写，支持 B站、抖音、小红书、YouTube 和播客；当前约 106 Star、12 Fork、11 次提交。它可以作为国内视频转写的备用实现，但采用门槛和社区验证度明显低于 last30days，不能作为全网搜索主引擎。

它默认把输出写在 Skill 目录下，并使用 `/tmp` 做工作目录；如果接入，必须改成外接 SSD KnowledgeBase 路径，并先审计配套 `video-download` 的下载行为。暂不直接安装其一键脚本。

## X / Twitter 状态

X 已经是海外路由的一等来源：`overseas` 快速档会请求 `reddit,x,web`。当前项目默认关闭隐式浏览器 Cookie，X 需要显式授权；没有授权时状态标记为 `requires_explicit_auth`，不会误报为“X 没有相关内容”。

## Windows 5070 转写状态

已加入 `skills/research-router/scripts/视频转文字.py`、ASCII 入口 `skills/research-router/scripts/transcribe.py`、Windows 启动脚本和 `requirements-windows.txt`。它只接受本地媒体文件，默认使用 CUDA + faster-whisper `large-v3-turbo`，生成词级时间戳 JSON、可阅读 Markdown 和 SRT。Windows RTX 5070 已实测 5191 秒中文视频耗时 108.7 秒（约 47.8 倍实时）；启动脚本会自动加入 CUDA DLL 路径。E 盘外置 SSD 的 KnowledgeBase 目录和哨兵已建立，正式落库仍需先把视频放入 `待转写`。

BibiGPT 会员和 API 暂不接入，后续若需要云端处理，再作为 `TranscriptProvider` 的可选实现加入。
