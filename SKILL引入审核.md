# 外部 Skill 引入审核流程

所有从 GitHub、插件市场或其他来源取得的 Skill，必须先放入 `~/projects`（当前路径实际位于外接 SSD 工作区），完成只读审核后才能决定是否接入。

## 流程

1. **登记来源**：记录仓库 URL、许可证、Star/Fork、最近提交、版本和下载时间。
2. **静态审计**：阅读 `SKILL.md`、安装脚本、依赖、MCP 配置和网络请求；搜索 Cookie、Token、代理、上传、发布、点赞、评论、关注、删除和高频循环。
3. **风险分级**：标记账号风控、验证码处理、隐式 Cookie 读取、数据外传、写操作、许可证和供应链风险。
4. **改造决策**：明确保留、删除或替换哪些能力；默认删除发布和互动能力，只保留资料搜索所需的只读路径。
5. **隔离验证**：使用独立虚拟环境、测试账号或公开页面运行最小用例；禁止把真实账号 Cookie 放进仓库。
6. **批准接入**：只有审计报告、改造记录和冒烟测试通过后，才复制到 Skill 目录或加入主项目。

## 小红书特别规则

- 不把“反检测、验证码处理、代理池、Cookie 导出”视为安全能力；它们是额外风控面，默认不启用。
- 优先使用 Ego Lite 的低频人工只读会话；第三方 MCP 只在审计后保留搜索和详情读取工具。
- 禁止自动点赞、收藏、评论、关注、发布、批量下载和高频翻页。
- 失败必须区分未登录、受限、限流、无结果和程序错误，不得通过提高频率绕过限制。

## 审核记录模板

| 项目 | 记录 |
|---|---|
| 仓库/版本 | |
| 许可证 | |
| Star/Fork/最近提交 | |
| 登录与 Cookie 方式 | |
| 网络请求与外传 | |
| 写操作 | |
| 风控/验证码/代理 | |
| 需要删除或改造的文件 | |
| 隔离测试结果 | |
| 最终决定 | 保留 / 改造后保留 / 不接入 |
